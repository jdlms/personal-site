---
import Layout from "../layouts/Layout.astro";

const resp = await fetch(Astro.url + "getBookmarks");

const bookmarks: any = await resp.json();
---

<Layout title="jdlms.dev">
  <h1>bookmarks</h1>

  <ul>
    {bookmarks.map((item: any) => <li>item={item}</li>)}
    <li></li>
  </ul>
</Layout>
